version: "3.9"

services:
  chicken-disease-classifier:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: chicken-disease-classifier

    ports:
      - "10000:10000"

    environment:
      PORT: 10000

    restart: unless-stopped